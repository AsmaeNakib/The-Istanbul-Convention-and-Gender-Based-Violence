# International Policy and Gender-Based Violence: The Istanbul Convention in Europe

Replication repository for the Master thesis of the same name.

**Author** Asmae Nakib · **Degree** Master Thesis · **School** School of
Business, Social & Decision Sciences, Constructor University ·
**Supervisor** Prof. Dr. Mandi Larsen · **Second Reader** Prof. Dr. Fabian Dehos

---

## 1. Project overview

The study asks whether ratifying the Council of Europe Convention on
Preventing and Combating Violence against Women and Domestic Violence (the
Istanbul Convention) is followed by a measurable change in gender-based
violence (GBV) outcomes in Europe.

The unit of analysis is the country-year. The panel covers 45 European
countries; the main estimation sample runs from 2000 to 2023. Treatment is the
calendar year in which a country ratified the Convention, dated from the
Council of Europe Treaty Office record; entry into force and first-full-year
codings are carried as alternative timings. Turkey is the panel's only
withdrawal, effective 1 July 2021; its 2021–2023 rows are dropped from the main
samples and restored as a robustness check.

The primary outcome is the female intentional homicide rate. Male homicide
serves as an additional outcome for comparison, and the log female/male ratio
as a within-country comparison. Recorded sexual violence, domestic violence legislation, GREVIO
monitoring and shelter capacity are used to describe implementation.

The main result is that no method finds a statistically significant decline in
female homicide after ratification, and that the design's minimum detectable
effect is large relative to the effects the literature would predict. The
manuscript reports this as a limit on what the data can establish, not as
evidence of no effect.

**Manuscript files.** `thesis text final.md` is the source of the thesis text.
`thesis text final.tex` is generated from it by `build_thesis.py`, so the two
always carry identical substantive content. Edit the Markdown, then run
`python build_thesis.py`.

---

## 2. Repository structure

```
ic-gbv-thesis/
├── README.md                     this file
├── thesis text final.md          manuscript (source of record)
├── thesis text final.tex         manuscript, LaTeX (generated by build_thesis.py)
├── thesis text final.pdf         compiled manuscript
├── build_thesis.py               Markdown -> LaTeX converter
├── requirements.txt              pinned computational environment
│
├── data/
│   ├── raw/                      the two source files the code opens directly
│   │                             (see section 3)
│   ├── interim/                  merged country-year panel before derivation
│   ├── gbv_panel_analysis.csv    analysis panel
│   ├── treatment_dates.csv       Convention dates used in the analysis
│   └── variable_sources.csv      where each built variable comes from
│
├── src/
│   ├── build_analysis_panel.py   raw sources -> analysis panel
│   ├── analysis_helpers.py       loading, sample construction, outcome registry
│   ├── estimators.py             two-way fixed effects, stacked DiD,
│   │                             Callaway-Sant'Anna, event studies, inference
│   └── scm.py                    synthetic control (feasibility check)
│
├── notebooks/
│   ├── 01_data_and_design.ipynb
│   ├── 02_main_analysis.ipynb
│   ├── 03_additional_outcomes_and_cases.ipynb
│   └── 04_robustness.ipynb
│
├── viz/
│   ├── thesis_style.py           shared figure style and save helper
│   └── make_thesis_figures.py    generates every figure into Figures/
│
├── Figures/                      19 figures as PNG, PDF and SVG; the thesis
│                                 embeds 14 of them, the rest are kept as
│                                 supporting material. Also FIGURE_GUIDE.md
│                                 and CAPTIONS.md
│
└── outputs/
    ├── tables/                   estimation results
    │   └── appendix_*.csv        consolidated exports, not printed in the thesis
    ├── diagnostics/              design and data-quality checks
    └── figures/                  figures written directly by the notebooks
```

---

## 3. Data

The analysis runs on three files, all included here: `data/gbv_panel_analysis.csv`
(the panel every notebook reads), `data/treatment_dates.csv` and
`data/variable_sources.csv`.

### 3.1 Files included in this repository

| File | Read by | Used for |
| --- | --- | --- |
| `data/gbv_panel_analysis.csv` | all four notebooks | every estimate in the thesis |
| `data/treatment_dates.csv` | `build_analysis_panel.py`, `make_thesis_figures.py` | signature, ratification, entry-into-force and withdrawal dates |
| `data/variable_sources.csv` | notebook `01` | source and construction of every built variable |
| `data/interim/base_country_year_panel.csv` | `build_analysis_panel.py` | merged country-year frame the panel build starts from |
| `data/raw/data_cts_intentional_homicide.xlsx` | `build_analysis_panel.py` | female and male homicide rates, intimate-partner/family homicide, and the United Kingdom rebuild |
| `data/raw/WBL2024 Safety data_Website.xlsx` | consulted directly | the domestic-violence legal criterion described in Section 5.7.1 |

`data_cts_intentional_homicide.xlsx` is the only raw file any script opens.

### 3.2 Sources already merged into the interim panel

The remaining sources were harmonised into
`data/interim/base_country_year_panel.csv` and are not re-hosted here. Download
them from the publishers if you want to rebuild that file from scratch.

| Source | Used for | Where to obtain it |
| --- | --- | --- |
| UNODC, Violent and Sexual Crime (UN-CTS) | recorded sexual violence and rape | <https://dataunodc.un.org/> |
| UNODC & UN Women femicide estimates | descriptive context | <https://dataunodc.un.org/> |
| World Bank, Women, Business and the Law | domestic-violence legislation and gender-related-killing indicators | <https://wbl.worldbank.org/> |
| World Bank, World Development Indicators | population, GDP per capita, female labour force participation | <https://data.worldbank.org/> |
| Worldwide Governance Indicators | government effectiveness, rule of law | <https://www.worldbank.org/en/publication/worldwide-governance-indicators> |
| WAVE Network country report | shelter capacity | <https://wave-network.org/> |
| GREVIO baseline evaluation reports | first baseline evaluation years | <https://www.coe.int/en/web/istanbul-convention/grevio> |

Treaty dates come from the Council of Europe registry for CETS No. 210 and are
stored in `data/treatment_dates.csv`. `data/variable_sources.csv` documents, for
every variable added or derived during the build, its source, how it was
constructed, and any note attached to it.

Sex-disaggregated UNODC homicide rates are per 100,000 of the population of that
sex: female homicide is per 100,000 women and male homicide per 100,000 men.
The United Kingdom, which UNODC reports as three separate jurisdictions, is
rebuilt on the same basis in `src/build_analysis_panel.py`.

---

## 4. Guide to the CSV files

### 4.1 Data

Listed in section 3.1. `gbv_panel_analysis.csv` is the analysis panel — outcomes,
treatment indicators and covariates — and is the only dataset the notebooks read.

### 4.2 Main results — `outputs/tables/`

| File | Type | Description | Used for |
| --- | --- | --- | --- |
| `main_results.csv` | regression results | all main estimates, every outcome × estimator | Table 3, Figure 4 |
| `main_twfe_estimates.csv` | regression results | two-way fixed effects, full detail | Section 5.2 |
| `main_stacked_did_estimates.csv` | regression results | stacked DiD, clean control sets | Section 5.2 |
| `main_callaway_santanna_estimates.csv` | regression results | Callaway–Sant'Anna aggregate ATT, both control groups | Table 3, Section 5.2 |
| `callaway_santanna_attgt_female_homicide.csv` | regression results | group-time ATT(g,t) | Section 5.2 |
| `callaway_santanna_by_cohort_female_homicide.csv` | regression results | cohort-level ATT | Section 5.2 |
| `callaway_santanna_dynamic_female_homicide.csv` | event-study results | dynamic ATT by event time | Section 5.2 |
| `event_study_female_homicide.csv` | event-study results | TWFE event study, primary outcome | Figure 5, Section 5.3 |
| `event_study_male_homicide.csv` | event-study results | event study, male homicide comparison | Figure C.2, Section 5.5 |
| `event_study_homicide_sex_ratio.csv` | event-study results | event study, log female/male ratio | Section 5.5 |
| `event_study_female_ipf_homicide.csv` | event-study results | event study, intimate-partner/family homicide | Figure C.1, Section 5.6.1 |
| `event_study_sexual_violence.csv` | event-study results | event study, recorded sexual violence | Figure 8, Section 5.6.2 |
| `mechanism_sexual_violence.csv` | regression results | log-specification estimates for recorded sexual violence | Section 5.6.2, Appendix D |
| `parallel_trends_sensitivity_bounds.csv` | robustness | Rambachan–Roth bounds at a range of *M* | supporting material, Section 4.6 |
| `inference_alternative_methods.csv` | robustness | cluster-robust, wild cluster bootstrap and permutation p-values | supporting material, Section 4.6 |
| `control_group_sensitivity.csv` | robustness | estimates under alternative control groups | Section 5.4 |
| `robustness_results.csv` | robustness | consolidated robustness family | Appendix B |
| `robustness_treatment_definition.csv` | robustness | ratification vs entry-into-force vs first-full-year coding; Turkey coding variants | Table 4, Section 5.4 |
| `robustness_window_and_transformation.csv` | robustness | alternative estimation windows and outcome transformations | Table 4, Appendix B |
| `robustness_weighting.csv` | robustness | population weighting and microstate exclusion | Appendix B |
| `robustness_balanced_event_window.csv` | robustness | balanced −6…+8 event window | Appendix B |
| `robustness_leave_one_treated_out.csv` | robustness | dropping each ratifying country in turn | Appendix B |
| `robustness_later_ratifiers.csv` | robustness | later-ratifier comparison: cohorts 2013–2019 against countries ratifying after each cohort's window | Table B.1, Sections 5.4 and 5.6 |
| `descriptive_femicide_law_event_history.csv` | descriptive | femicide-law adoption event history | Section 5.7.2 |
| `turkey_placebo_reassignment.csv` | robustness | placebo withdrawal reassigned to each non-withdrawing country | Section 5.9 |
| `scm_spain_series.csv` | synthetic-control results | treated and synthetic series, Spain | Appendix E.1 |
| `scm_spain_donor_weights.csv` | synthetic-control results | donor weights, Spain | Appendix E.1 |
| `scm_spain_diagnostics.csv` | synthetic-control results | pre/post RMSPE, Spain | Appendix E.1 |
| `scm_spain_placebo_in_space.csv` | synthetic-control results | placebo-in-space distribution, Spain | Appendix E.1 |
| `scm_spain_leave_one_donor_out.csv` | synthetic-control results | donor-robustness check, Spain | Appendix E.1 |
| `scm_albania_series.csv`, `scm_albania_donor_weights.csv` | synthetic-control results | Albania fit and weights | Appendix E.1 |
| `scm_serbia_series.csv`, `scm_serbia_donor_weights.csv` | synthetic-control results | Serbia fit and weights | Appendix E.1 |
| `scm_diagnostics.csv` | synthetic-control results | fit statistics across attempted cases | Appendix E.1 |
| `scm_in_hull_countries.csv` | synthetic-control results | which treated countries lie inside the donor convex hull | Appendix E.1 |
| `appendix_a1_sensitivity.csv` | export | sensitivity of the main estimate | repository export, not printed in the thesis |
| `appendix_a2_all_estimates.csv` | export | every estimate with its permitted interpretation | repository export, not printed in the thesis |
| `appendix_b1_country_coverage.csv` | export | pre/post observation counts by country and outcome | repository export, not printed in the thesis |
| `appendix_c1_design_diagnostics.csv` | export | design checks | repository export, not printed in the thesis |
| `appendix_c2_implementation_by_country.csv` | export | implementation indicators by country | repository export, not printed in the thesis |

### 4.3 Diagnostics — `outputs/diagnostics/`

| File | Type | Description | Used for |
| --- | --- | --- | --- |
| `analysis_sample_inventory.csv` | descriptive statistics | rows, countries and cohorts in every analysis sample | Section 3.6 |
| `data_schema_missingness.csv` | descriptive statistics | column types and missingness across the panel | Section 3.3 |
| `outcome_data_quality.csv` | descriptive statistics | coverage, gaps and volatility by outcome | Section 3.6 |
| `outcome_method_permissions.csv` | reference | which methods each outcome supports, recorded before estimation | Section 4.5 |
| `pretreatment_balance.csv` | descriptive statistics | pre-ratification balance, ratifiers vs control countries | Sections 2.5 and 6.3 |
| `treatment_timing_by_year.csv` | descriptive statistics | ratification cohorts by year | Figure 2, Section 3.4 |
| `design_diagnostics.csv` | diagnostics | consolidated design checks | build check |
| `minimum_detectable_effects.csv` | diagnostics | MDE at 80% power, absolute and as a share of the treated mean | Section 5.2 |
| `leave_one_control_out.csv` | robustness | dropping each control country in turn | Figure 6, Section 5.4 |
| `placebo_timing_tests.csv` | robustness | treatment shifted four years earlier | Figure E.2, Section 5.4 |
| `pretrend_tests.csv` | diagnostics | linear pre-ratification trend tests, all outcomes | Sections 5.3 and 5.6 |
| `pretrend_linear_vs_event_study.csv` | diagnostics | linear test compared with joint event-study test | Section 4.3 |
| `pretrend_event_study_*.csv` | diagnostics | pre-period event-study coefficients per outcome | Section 4.3 |
| `standard_error_check.csv` | diagnostics | screen for invalid or degenerate standard errors | build check |
| `later_ratifier_stack_composition.csv` | diagnostics | treated and comparison countries entering each later-ratifier stack | Section 4.6, Appendix B |
| `scm_feasibility.csv` | diagnostics | donor-pool common support per treated country | Appendix E.1 |
| `software_manifest.csv` | reference | package versions at run time | reproducibility |
| `runtime.txt` | reference | interpreter, platform, data path and random seed | reproducibility |

---

## 5. Methods

- **Two-way fixed effects DiD** — country and year fixed effects, standard
  errors clustered by country. Baseline specification.
- **Stacked DiD** — one clean event window per ratification cohort, so an
  already-ratified country never serves as a comparison.
- **Callaway and Sant'Anna group-time ATT** — reported against both
  not-yet-treated and never-treated control groups.
- **Event studies** — relative-time coefficients with binned endpoints and
  contributing-country counts reported alongside.
- **Male homicide comparison** — male homicide as an additional outcome, and
  the log female/male ratio, which removes changes affecting both sexes.
- **Parallel-trends sensitivity** — Rambachan and Roth (2023) bounds.
- **Inference** — cluster-robust, wild cluster bootstrap (Webb weights, 999
  replications) and a permutation test (999 reassignments); all three are
  reported rather than reconciled. Holm correction within the confirmatory
  family.
- **Power** — minimum detectable effect at 80% power, expressed as a share of
  the treated mean.
- **Placebo timing** — treatment shifted four years earlier, screened at
  p = 0.10.
- **Synthetic control** — used only as a feasibility check on the control
  countries (pre-period RMSPE against the outcome's own standard deviation),
  not as an effect estimate.

A table of permitted methods
(`outputs/diagnostics/outcome_method_permissions.csv`) records which estimators
each outcome supports, and is written before any estimate is produced.

---

## 6. Main findings

- Two-way fixed effects gives +0.302 deaths per 100,000 women (SE 0.200,
  p = 0.140, N = 858). Stacked DiD gives +0.108 (SE 0.114, p = 0.349) and
  Callaway–Sant'Anna +0.117 against not-yet-treated countries (SE 0.173,
  p = 0.477). None is statistically significant.
- The minimum detectable effect for female homicide at 80% power is 0.56 deaths
  per 100,000 women, about 56% of the treated mean. The design cannot rule out
  the smaller changes the literature would predict.
- Male homicide moves in the same direction (+0.756, p = 0.168) and the log
  female/male ratio is flat (−0.054, p = 0.511), so the female series does not
  move distinctively.
- The result is not stable against the composition of the control group:
  dropping Lithuania alone moves the estimate by roughly half.
- A later-ratifier comparison that removes the permanent non-ratifiers
  altogether, and compares the 2013–2019 cohorts only with countries ratifying
  after each cohort's window, gives +0.007 (p = 0.970). The magnitude depends on
  the counterfactual; the qualitative reading does not.
- The three inference procedures disagree (p = 0.140, 0.170, 0.041); the
  disagreement is reported as a feature of a design with a small, structurally
  distinct control group.
- Implementation indicators diverge sharply from treaty status, which is the
  substantive point: a binary ratification indicator carries limited
  information about what a country has actually put in place.

---

## 7. Reproducibility

```bash
pip install -r requirements.txt      # Python 3.13.3

python src/build_analysis_panel.py   # data/gbv_panel_analysis.csv

# then, in order:
jupyter lab notebooks/01_data_and_design.ipynb
jupyter lab notebooks/02_main_analysis.ipynb
jupyter lab notebooks/03_additional_outcomes_and_cases.ipynb
jupyter lab notebooks/04_robustness.ipynb

python viz/make_thesis_figures.py    # regenerates every file in Figures/
python build_thesis.py               # regenerates "thesis text final.tex"
```

The panel build runs from the files included here: it reads
`data/interim/base_country_year_panel.csv` and
`data/raw/data_cts_intentional_homicide.xlsx` and writes
`data/gbv_panel_analysis.csv` and `data/variable_sources.csv`. Rebuilding the
interim panel itself needs the sources listed in section 3.2.

The notebooks must run in order: `01` writes the sample inventory and the
table of permitted methods that `02`–`04` read. Each notebook then runs
independently from a fresh kernel. Every estimation step is seeded
(`20260804`, recorded in `outputs/diagnostics/runtime.txt`), so the bootstrap
and permutation p-values reproduce exactly.

All four notebooks were re-run end to end against this repository state and the
reported numbers reproduce. Package versions actually used are recorded in
`outputs/diagnostics/software_manifest.csv`; `csdid` 0.2.9 supplies the
Callaway–Sant'Anna estimator, and `pyfixest` and `linearmodels` are not
required.

```bash
pdflatex "thesis text final.tex"   # run three times
```

`thesis text final.tex` compiles with pdfLaTeX from the directory containing it,
with `Figures/` alongside. It was built and verified with MiKTeX 25.12 /
pdfTeX 4.23: **73 pages, no errors, no warnings, no overfull or underfull
boxes**. Figures number 1–10 in the body and C.1, C.2, E.1, E.2 in the
appendices; tables number 1–5 in the body and B.1 in Appendix B; every in-text
cross-reference matches.

The preamble uses `newtxtext`/`newtxmath` for Times-compatible type; a
commented `fontspec` block switches to system Times New Roman under XeLaTeX or
LuaLaTeX. Dropping a `msc-logo.pdf` (or `.png`) in this directory adds the
university logo to the title page; without it the page compiles unchanged.
